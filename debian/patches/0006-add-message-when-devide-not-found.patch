Index: guvcview/guvcview/guvcview.c
===================================================================
--- guvcview.orig/guvcview/guvcview.c
+++ guvcview/guvcview/guvcview.c
@@ -41,6 +41,7 @@
 #include "config.h"
 #include "gui.h"
 #include "core_io.h"
+#include <gtk/gtk.h>
 
 int debug_level = 0;
 
@@ -81,6 +82,7 @@ void signal_callback_handler(int signum)
 int main(int argc, char *argv[])
 {
 	/*check stack size*/
+	gtk_init(&argc, &argv);
 	const rlim_t kStackSize = 128L * 1024L * 1024L;   /* min stack size = 128 Mb*/
     struct rlimit rl;
     int result;
@@ -247,8 +249,17 @@ int main(int argc, char *argv[])
 	if(!vd)
 	{
 		char message[55];
-		snprintf(message, 54, "no video device (%s) found", my_options->device);
-		gui_error("Guvcview error", message, 1);
+		snprintf(message,54,"Câmera não encontrada em: (%s) ", my_options->device);
+		GtkWidget *dialog;
+		dialog = gtk_message_dialog_new(NULL,
+						GTK_DIALOG_DESTROY_WITH_PARENT,
+						GTK_MESSAGE_ERROR,
+						GTK_BUTTONS_CLOSE,
+						"%s",message);
+
+		gtk_window_set_title(GTK_WINDOW(dialog), "Error");
+		gtk_widget_destroy(dialog);
+
 		options_clean();
 		return -1;
 	}
